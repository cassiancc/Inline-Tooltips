### Added
- New config option to never expand the tooltips.
- Attributes in the `inline_tooltips:shows_percentage` attribute tag will be displayed as percentages, unless the `checkPercentageTag` config is disabled. By default, knockback resistance is included in this tag, and it will also always be displayed with ReArm installed.